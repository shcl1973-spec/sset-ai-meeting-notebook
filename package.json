{
  "name": "sset-ai-meeting-notebook",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "prepare:capacitor": "node scripts/prepare-capacitor.js",
    "build:android:debug": "npm run prepare:capacitor && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
